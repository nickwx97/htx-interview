# Environment variables for backend
# Keyframe sampling: process every Nth frame (integer)
VIDEO_SAMPLE_RATE=5
# Scene-change detection threshold: mean pixel diff between sampled frames (float)
VIDEO_DIFF_THRESH=30.0
