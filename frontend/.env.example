# Example environment for docker-compose
# When running docker-compose locally on Linux, use the backend service name
VITE_API_BASE=http://backend:8000

# On Docker Desktop (Mac/Windows):
# VITE_API_BASE=http://host.docker.internal:8000
